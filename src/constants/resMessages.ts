enum ResMessages {
  BAD_REQUEST = "Bad request.",
  INVALID_UUID = "Invalid UIID.",
  NOT_FOUND = "Not found.",
  INVALID_DATA = "Invalid request's body.",
  DELETED = "The data was deleted."
}

export default ResMessages;